# Xandeum pRPC Endpoints (comma-separated)
# These are the endpoints used to fetch pNode data from the network
NEXT_PUBLIC_PRPC_ENDPOINTS=https://prpc.xandeum.network,https://rpc.xandeum.network

# Optional: Override the default cache TTL (in milliseconds)
# NEXT_PUBLIC_CACHE_TTL=30000

# Optional: Site URL for SEO
NEXT_PUBLIC_SITE_URL=https://xandeum-analytics.vercel.app
